copy_tree:
  directories:
    - [$(stage_dir), $(run_dir)]
ewok_template:
  template:
    pattern: DOUBLE_CURLY_BRACES
  directories:
    - [ $(stage_dir)/input.nml.tmpl, $(run_dir)/input.nml ]
    - [ $(stage_dir)/model_configure.tmpl, $(run_dir)/model_configure ]
    - [ $(stage_dir)/diag_table.tmpl, $(run_dir)/diag_table ]
