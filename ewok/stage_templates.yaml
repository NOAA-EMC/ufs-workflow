template_files:
  root: ..
  destination: stage_dir
  template:
    pattern: AT_SQUARE_BRACES
    dictionaries:
      - $(suite_dir)/$(experiment.yaml)
  directories:
    - [../files/control.nml.IN, input.nml]
    - [../files/model_configure.IN, model_configure]
    - [../files/nems.configure, nems.configure]
