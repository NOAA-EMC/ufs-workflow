# files to copy
copy_files:
    # source is relative to the model root (fv3-jedi)
    - source: {model_repos}/tests
      target: fieldsets
      files:
        - default_vars.sh
    - source: {model_repos}/tests/fv3_conf
      target: fieldsets
      files:
        - ccpp_control_run.IN
        - model_configure
    - source: {model_repos}/tests/fv3_conf
      target: fv3files
      files:
        - diag_table
        - data_table
        - field_table
